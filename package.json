{
  "name": "root",
  "version": "4.1.0",
  "license": "MIT",
  "private": "true",
  "devDependencies": {
    "@mach/eslint-config": "*",
    "@mach/prettier-config": "*",
    "@types/jest": "^26.0.8",
    "@types/node": "^14.0.27",
    "jest": "^26.2.2",
    "ts-jest": "^26.1.4",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "build": "yarn lerna run build",
    "test": "yarn jest",
    "front": "yarn workspace @mach/front"
  },
  "workspaces": {
    "packages": [
      "packages/**/*"
    ]
  }
}
