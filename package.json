{
  "name": "root",
  "version": "4.1.0",
  "license": "MIT",
  "private": "true",
  "devDependencies": {
    "@mach/eslint-config": "*",
    "@mach/prettier-config": "*",
    "@types/jest": "^26.0.8",
    "@types/node": "^15.0.1",
    "jest": "^26.2.2",
    "lerna": "^4.0.0",
    "ts-jest": "^26.1.4",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "build": "yarn lerna run build",
    "test": "yarn jest",
    "front": "yarn workspace @mach/front"
  },
  "workspaces": {
    "packages": [
      "packages/**/*"
    ]
  }
}
